apiVersion: tekton.dev/v1beta1
kind: PipelineRun
metadata:
  name: user-pr
  namespace: tekton-pipelines
spec:
  serviceAccountName: tekton-sa
  pipelineRef:
    name: user-pipeline
  resources:
  - name: repo
    resourceRef:
      name: level3-project
