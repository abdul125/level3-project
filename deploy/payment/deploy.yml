apiVersion: apps/v1
kind: Deployment
metadata:
  name: 
  labels:
    app: payment
    type: 
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payment
  template:
    metadata:
      labels:
        app: payment
    spec:
      containers:
      - name: payment
        image: abdul125/payment
        ports:
        - containerPort: 
