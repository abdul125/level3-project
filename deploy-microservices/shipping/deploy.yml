apiVersion: apps/v1
kind: Deployment
metadata:
  name: 
  labels:
    app: shipping
    type: 
spec:
  replicas: 1
  selector:
    matchLabels:
      app: shipping
  template:
    metadata:
      labels:
        app: shipping
    spec:
      containers:
      - name: shipping
        image: abdul125/shipping
        ports:
        - containerPort: 
